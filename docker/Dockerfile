FROM grafana/grafana:latest

# Optional: copy custom config files, plugins, or dashboards
# COPY custom.ini /etc/grafana/grafana.ini
# COPY dashboards/ /etc/grafana/provisioning/dashboards/

# Expose port 3000
EXPOSE 3000
